{
  "name": "echo-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Echo backend — sycophancy detection for LLMs. Adversarial variant generation (Claude), parallel execution across OpenAI/Anthropic/Google, and consistency scoring.",
  "scripts": {
    "start": "node dev-server.mjs",
    "dev": "node dev-server.mjs",
    "deploy": "vercel --prod",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.88.0",
    "@google/genai": "^0.15.0",
    "openai": "^4.77.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@vercel/node": "^3.2.29",
    "typescript": "^5.7.0",
    "vercel": "^39.0.0"
  }
}
